#we will install docker on our linux machine
  #from the windows jump host login to your linux machine
  sudo -i
  ##you need to be a root user to install all the packages of docker
  ##then go to the below mentioned site and install docker complete step 1 and step 2
  https://docs.docker.com/engine/install/ubuntu/
  ###after that 
  docker
  ####it will show docker is installed 
